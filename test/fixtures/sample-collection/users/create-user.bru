meta {
  name: Create User
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/users
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "name": "{{userName}}",
    "email": "user@example.com",
    "nested": { "key": "value" }
  }
}

auth:bearer {
  token: {{token}}
}
