meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/login
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "{{userName}}",
    "password": "{{apiKey}}"
  }
}
